<UserControl x:Class="RibbonDemo02.Views.Migration"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:avalon="clr-namespace:ICSharpCode.AvalonEdit;assembly=ICSharpCode.AvalonEdit"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- TreeView for the directories and files -->
        <!--<TreeView Name="DirectoryTreeViewMigration" Grid.Column="0" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>-->

        <!-- AvalonEdit TextEditor -->
        <!--<avalon:TextEditor 
            Name="TextEditorMigration"
            Grid.Column="1"
            VerticalAlignment="Stretch"
            HorizontalAlignment="Stretch"
            ShowLineNumbers="True"
            IsReadOnly="True"
            SyntaxHighlighting="XML"
            VerticalScrollBarVisibility="Visible"
            HorizontalScrollBarVisibility="Visible" />-->
    </Grid>
</UserControl>
