<UserControl x:Class="RibbonDemo02.Views.DatabaseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:RibbonDemo02.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- TreeView for the directories and files -->
        <TreeView Name="DirectoryTreeView" Grid.Column="0" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>

        <!-- TextBox for displaying file contents -->
        <TextBox Name="FileContentTextBox" Grid.Column="1" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" IsReadOnly="True"/>
    </Grid>
</UserControl>
